<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="edu.band148.wgumanager">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WGUManager">
        <activity android:name=".EditTermActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.WGUManager.NoActionBar"></activity>
        <activity android:name=".EditCourseActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.WGUManager.NoActionBar"></activity>
        <activity android:name=".EditAssessmentActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.WGUManager.NoActionBar"></activity>
        <activity android:name=".EditInstructorActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.WGUManager.NoActionBar"></activity>
        <activity
            android:name=".TermActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.WGUManager.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".CourseActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.WGUManager.NoActionBar"></activity>
        <activity
            android:name=".AssessmentActivity"
            android:label="@string/app_name"
            android:theme="@style/Theme.WGUManager.NoActionBar"></activity>
    </application>

</manifest>